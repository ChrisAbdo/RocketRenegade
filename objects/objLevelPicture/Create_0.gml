//Set level to rmLevelSelect by default, level to locked by default, and sprite to lock icon by default
levelLocked = true;
level = rmLevelSelect;
sprite = sprLevelLocked;

//Trigger alarm to update level sprite next frame
alarm[0] = 1;